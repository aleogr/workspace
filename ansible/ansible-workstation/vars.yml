# Usuário
new_user: "aleogr"
user_shell: "/bin/bash"

# Sistema
debian_codename: "trixie"

# Hardware (Use o ID do WD SN850X)
disk_device: "/dev/disk/by-id/nvme-WD_BLACK_SN850X_2000GB_222503A00551"
# disk_device: "/dev/sdb" # Para testes

# ZFS
pool_name: "tank"
storage_id_vm: "VM-Storage"
zfs_arc_limit_bytes: 8589934592 # 8GB em Bytes

# Backup (PBS)
pbs_datastore: "Backup-PBS"
pbs_user: "root@pam"

# CPU
cpu_governor: "powersave"
