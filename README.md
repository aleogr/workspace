# workspace

01-system-base.sh: Arruma repositórios e instala base (Seguro pra VM).
02-desktop-gui.sh: Instala XFCE/Chromium (Seguro pra VM).
03-hardware-tune.sh: Aplica IOMMU, MSRs fix e ZFS RAM Limit (Rodar no PC Real).
04-storage-zfs.sh: Formata o disco de dados com Criptografia e TRIM (Rodar no PC Real após reboot).
05-final-polish.sh: Ajusta Swap (Seguro pra VM).
